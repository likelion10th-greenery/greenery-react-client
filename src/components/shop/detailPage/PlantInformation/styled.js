import styled from 'styled-components';

export const Wrapper = styled.div``;

export const Title = styled.p``;

export const Price = styled(Title);

export const Delivery = styled(Title);

export const Amount = styled.div``;
